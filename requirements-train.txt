# The training environment, installed SEPARATELY from the app (see README).
# Install the torch build that matches your machine FIRST, then this file.
#
#   CPU only : pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Training falls back to CPU automatically when no GPU is present (--device auto).
torch>=2.0
torchvision>=0.15
ultralytics>=8.3
numpy>=1.24
opencv-python-headless>=4.8   # only used by the optional --preprocess image enhancement
pyyaml>=6.0
tqdm>=4.65
scikit-learn>=1.3             # validator precision/recall/F1 reporting
